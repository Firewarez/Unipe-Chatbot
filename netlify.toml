[build]
base = "frontend"
command = "npm run build"
publish = ".next"

[build.environment]
NODE_VERSION = "20"

# Configure no painel da Netlify:
# NEXT_PUBLIC_API_URL=https://seu-backend.onrender.com/api/chat
